### Winglogbeat Docker Container

This is a Dockerfile to build a container for running on Azure AKS, specifically in order to run winlogbeat template and dashboard imports against an Elastic Stack instance.
